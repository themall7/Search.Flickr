var app = app || {};

// sub namespace
app.config = {
    timeout : {
        default: 1000,
        submit: 100,
    },
};

app.form = {
    tag4val: 'span',
    class4val: '.control-label',
    msgbox: '#message_box',
};