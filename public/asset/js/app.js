var app = app || {};

// sub namespace
app.config = {
    timeout : {
        default: 3000,
        submit: 100,
    },
};

app.form = {
    tag4val: 'span',
    class4val: '.validation-advice',
    msgbox: '#message_box',
};